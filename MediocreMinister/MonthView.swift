import SwiftUI

struct MonthView: View {
    var body: some View {
        Text("Hello, Ari!")
            .padding()
    }
}

struct MonthView_Previews: PreviewProvider {
    static var previews: some View {
        MonthView()
    }
}
