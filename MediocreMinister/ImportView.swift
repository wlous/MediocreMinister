import SwiftUI

struct ImportView: View {
    var body: some View {
        Text("Hello, Ari!")
            .padding()
    }
}

struct ImportView_Previews: PreviewProvider {
    static var previews: some View {
        ImportView()
    }
}
