import SwiftUI

struct ProfileView: View {
    var body: some View {
        Text("Hello, Ari!")
            .padding()
    }
}

struct ProfileView_Previews: PreviewProvider {
    static var previews: some View {
        ProfileView()
    }
}
